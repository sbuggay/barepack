Handling of actions in components (pass down by prop?)
Handling of store in components (pass down by prop?)
Organization of redux components(?) vs react components
How are <select>s going to work with react-redux? (onChange?)
Organization of ActionTypes.ts?
Layout of action definitions in TS
Semicolons in .tslint
Better explanation of how reducers change state efficiently. 
    Is cloning the entire state with only the change really the best way?
